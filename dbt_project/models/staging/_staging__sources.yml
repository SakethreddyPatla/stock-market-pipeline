version: 2

sources:
  - name: raw
    database: STOCK_ANALYTICS
    schema: RAW 
    tables:
      - name: daily_prices_raw
        description: "Raw JSON response from Alpha Vantage TIME_SERIES_DAILY endpoint"
        columns:
          - name: symbol
            description: "Stock ticker symbol"
            tests: 
              - not_null
          - name: api_response
            description: "Full JSON API response stored as variant"